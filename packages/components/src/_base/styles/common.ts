import type { SerializedStyles } from "@emotion/react";

import { css } from "@emotion/react";

export const fullWidth: SerializedStyles = css({
  width: "100%"
});

export const fullHeight: SerializedStyles = css({
  height: "100%"
});

export const fullSize: SerializedStyles = css({
  width: "100%",
  height: "100%"
});
